<?xml version="1.0"?>
<block>
  <name>Antenna Correction</name>
  <key>doa_antenna_correction</key>
  <category>DoA</category>
  <import>import doa</import>
  <make>doa.antenna_correction($num_inputs, $config_filename)</make>

  <param>
    <name>Num Inputs</name>
    <key>num_inputs</key>
    <type>int</type>
  </param>
  <param>
    <name>Config File</name>
    <key>config_filename</key>
    <value>/tmp/antenna.cfg</value>
    <type>file_open</type>
  </param>

  <sink>
    <name>in</name>
    <type>complex</type>
    <nports>$num_inputs</nports>
  </sink>

  <source>
    <name>out</name>
    <type>complex</type>
    <nports>$num_inputs</nports>
  </source>
    <doc>
This block uses antenna gain and phase values stored in a config file to scale the autocorrelation matrix such that the response of the antennas in the array are homogenous.
Args:
num_inputs: Num. of elements in the antenna array.
config_filename: Config file that has been generated by one of the antenna calibration blocks. 
  </doc>
</block>
